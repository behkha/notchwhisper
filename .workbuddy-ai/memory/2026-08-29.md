# 2026-08-29

## README overhaul (NotchWhisper)
- Audited full source; rewrote README.md to document previously-undocumented
  features: Local LLM post-processing, Custom Dictionary, Transcript History, 6 themes.
- Honest License section (no LICENSE file present). Noted "Behavior" prefs exist in
  Settings.swift but aren't exposed in the Settings UI.

## Universal build + .dmg + GitHub Releases (NotchWhisper)
- `build.sh`: added `UNIVERSAL=1` flag → `swift build --arch arm64 --arch x86_64`
  (cross-compiles x86_64 from Apple Silicon) + `lipo -info` verification.
- Added `make_dmg.sh`: hdiutil-based universal .dmg (app + /Applications symlink),
  named by CFBundleShortVersionString. No Homebrew deps.
- Added `.github/workflows/release.yml`: on `v*` tag push or manual dispatch,
  builds universal .dmg on macos-latest and uploads to GitHub Release via
  softprops/action-gh-release.
- README: added "Releasing a .dmg (GitHub Releases)" section.
- `.gitignore`: added `*.dmg`.
- Validated script syntax (bash -n). Did NOT run full release build (WhisperKit
  compile is ~20-40 min).
